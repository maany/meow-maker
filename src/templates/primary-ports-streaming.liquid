import { BaseAuthenticatedInputPort, BaseStreamingOutputPort } from "{{ imports.cats }}/primary-ports";
import { {{ feature.CamelCase }}Response, {{ feature.CamelCase }}Request, {{ feature.CamelCase }}Error } from "{{ core_import_path }}/usecase-models/{{ feature_kebab_case }}-usecase-models";
import { {{ viewModel.name }} } from "{{ viewModel.importPath }}";
/**
 * @interface {{ feature.CamelCase  }}InputPort that abstracts the usecase.
 */
export interface {{ feature.CamelCase }}InputPort extends BaseAuthenticatedInputPort<{{ feature.CamelCase }}Request> {}

// TODO: Add viewmodel
/**
 * @interface {{ feature.CamelCase }}OutputPort that abtrsacts the presenter
 */
export interface {{ feature.CamelCase }}OutputPort extends BaseStreamingOutputPort<{{ feature.CamelCase }}Response, {{ feature.CamelCase }}Error, {{ viewModel.name  }}> {}
