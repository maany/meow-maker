import { BaseErrorResponseModel, BaseResponseModel } from "{{ imports.cats }}/usecase-models"
/**
 * @interface {{ feature.CamelCase }}Request represents the RequestModel for {{ feature.snake_case }} usecase
*/
export interface {{ feature.CamelCase }}Request {
// TODO : add fields
}

/** 
 * @interface {{ feature.CamelCase }}Response represents the ResponseModel for {{ feature.snake_case }} usecase
*/
export interface {{ feature.CamelCase }}Response extends BaseResponseModel {
// TODO : add fields
}


/**
* @interface {{ feature.CamelCase }}Error represents the ErrorModel for {{ feature.snake_case }} usecase
*/
export interface {{ feature.CamelCase }}Error extends BaseErrorResponseModel {
// TODO : add fields
}